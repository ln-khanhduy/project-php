# Database Configuration
DB_HOST=localhost
DB_NAME=phone_store
DB_USER=root
DB_PASS=your_password

# SMTP Configuration
SMTP_USE_SMTP=true
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USERNAME=your_email@example.com
SMTP_PASSWORD=your_smtp_password
SMTP_TIMEOUT=60

# Google OAuth
# Hướng dẫn: 
# 1. Truy cập https://console.cloud.google.com/
# 2. Tạo OAuth 2.0 Client ID (Type: Web Application)
# 3. Thêm Authorized redirect URIs: http://localhost/project-php/public/oauth_callback.php (hoặc domain thực tế)
# 4. Copy Client ID và Client Secret dưới đây
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Site Configuration
SITE_NAME=PhoneStore
SITE_URL=http://localhost/project-php
MAIL_FROM_EMAIL=noreply@phonestore.local
